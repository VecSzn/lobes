# 决定记录

一行一条，倒序。写这个是因为过两周我自己都会忘了当时为什么这么选。

- 2026-09-15 晚，误差棒把结论改小了。证人版四处改动（验证叶不再投票、token 上限按生成的算、图片题定案必须有 perception、代码例子认散文里的 `=>` `==`）跑完两档全量，中档第一遍 308/370、在裸 9B 跑的那 320 道上 272 对 268，看上去是成绩和成本同时赢；于是拿同一份代码、同一台机、同一组 llama-server 进程又跑了一遍中档（18:30 跑完第一遍，18:31 起第二遍），得 303/370、320 道上 266。**268 夹在 272 和 266 中间，高档的 267 也夹在中间，所以成绩上三个条件谁也没赢谁，拆开的理由只剩成本。** 370 道里 11 道翻面（八道对变错、三道错变对），最终答案字符串只有 224 道一样；逐题库的波动是 tools 0、gsm8k 1、simpleqa 1、ocrbench 1、humaneval 2、multistep 2。成本反过来很稳：两遍 320 道上 token 7,887 对 7,813、秒 34.0 对 34.3，都差 0.9%，对裸 9B 的 13,436 / 62.2 是 59% 和 55%。穿过这个波动带活下来的只有两条：tools 两遍都 30/30 对 25/30 且两遍逐题零分歧，以及 SimpleQA 的弃答行为（9B 全答错 25，这边弃答 25 和 24、答错 4 和 5）。根因是 llama-server 四个并发槽改变批次组成，浮点归约跟着变，同一个种子不锁死同一串 token（两档之间 260 道里 140 道证人值不同、138 道判定相同）。README 和 REPORT 都改成两遍并列、成绩打平、成本领先的写法；挑跑得好的那一遍发就是给 benchmark 做特殊优化的另一种形式。三个种子会给出标准差而不是区间，租的机时只够两遍。八条预注册假设仍是四条不成立（第二遍逐条核对过，没有一条改判，但 W1、W2、W8 都落在波动带里，只是两遍都没破而已）。高档有 9 道图片题被 llama-server 报 500：2B 的四个槽共用一个 16384 上下文，图片加 16000 思考预算四路装不下；那 9 条记录挪到 ocrbench-500-errors.jsonl，同代码同并发补跑（评测按 suite+id 跳过已有记录，只补这 9 道），记在 REPORT 偏离 19。
- 2026-09-15 晚，文档整理。README 只写现在的系统：成绩表的列叫裸 9B / Lobes 中档 / Lobes 高档，不再有版本代号；里程碑一节撤了，每一版在放大后题库上的数字挪到 REPORT 附录的表里；中文版里脑叶一律用英文名（executive、perception、reasoning、motor、verifier、language）。ARCHITECTURE 重写成现在的设计，第一稿怎么想的、后来怎么改的不再夹在里面（那些在本文件）。REPORT 改成当前结果在前、假设、偏离、早先版本做附录。lobes.yaml 的 verifier 槽删了：09-15 傍晚起它是代码（lobes/lobe/verifier.py），没有一行代码读这个槽。量证人样本的 exp.py 进 eval/，加了 replay 子命令，回放 09-15 傍晚那两组测量的数字和当时一样（gsm8k 冷样本独自 185/200，正式规则 183；ocrbench perception 思考领头 35/50）。
- 2026-09-14 executive 换成 granite-4.0-h-1b（Q8_0，CPU 常驻）。分类题 341 条（六个题库的题面加 21 条自己写的闲聊/代码/常识/工具题，判对的标准是运行时会走哪条路）：LFM2.5-1.2B 换了八种问法最多对 157（三个是非题 12、单字母 157、定义加例子 89，应用题判成闲聊或代码），是模型不是提示词；granite-1b 定义加例子 330、granite-h-micro 331、qwen3.5-2b 327、qwen3.5-4b 335、gemma4-e2b 320；LiquidAI 的 LFM2.5-Encoder-350M-Prompt-Router 零样本 309 且要 torch，230M/350M 本体没有分类头。定义写严一版（code = 交付源码、needs_tool = 交付程序算出的结果）三家都没涨（325/328/328），剩下十来条错各家一样（"反转句子里的单词"、字符串哈希、写文件再读判成写代码，SimpleQA 日期题判成要工具），提示词到此为止，不再往题库上调。选 1B 而不是已装在 GPU 上的 3B/4B：4070 上四个 GPU 模型本来就装不下要换入换出，分类器留在 CPU 就不参与换，一条 0.85 s、不占显存；和 motor 同家，但 executive 不当证人。
- 2026-09-14 intake 只让模型分类，不再有规则：之前"goal 里有工具动词就把 code 降成 qa"那条删了（它是给 1.2B 擦屁股的），schema 是 {kind: chat|math|code|qa, needs_tool}，math 必须留在枚举里（去掉它整个坍成 code），kind=math 直接算 needs_tool。顺手修的三处：gemma4 模板 enable_thinking 默认开，llama-server 每次语言叶调用先偷偷想 900 token，现在每个调用都显式发 chat_template_kwargs.enable_thinking，且 schema 写进 system 提示（只靠语法约束 gemma 会答 "}54"）；答案只是一个裸数值时语言叶不改写；改写以 } ] ) > : ; , 开头的丢掉。5090 上 v3 中档 tools 从第二版的 29/30 掉到 17/30，13 条错里 11 条是 intake 标成 code（答案就变成一段程序），2 条是 gemma 的语法垃圾，都在这里。

- 2026-09-14 v3：共享黑板换成证人。5090 的 trace 里 gsm8k 1252/413、multi-03/06/08 都是同一条路：执行叶（1.2B）的计划文本和照它跑出的工具输出摆给了所有叶，验证器"盲解"看得见这些就回声，答案又在每叶重新生成成自由文本丢值。现在每个证人只拿题面（图片题加带来源的描述和 OCR 行），motor 从题面写程序、推理叶思考后交复算程序、程序打印的就是该证人的值，两个一致定案（有程序跑过是 evidence，没有是 consistency）；不一致才叫第三个证人，故意选另一个家族（gemma）而不是推理叶再抽一次，错误相关性低。没有 RETRY / VERIFY_WITH_TOOL / CONFLICT 循环，程序挂了只带 stderr 修一次；定下的值原样携带到语言叶，改写丢了数字或一行就丢改写。单题硬上限：证人数、调用数、token、秒，撞上就带 hedge 交卷。梯子开着时 9B / 远端是多一个证人，不是接管。规则先写在 eval/PREREG-v3.md 再动的代码。
- 2026-09-14 发布内容：仓库只放代码、题目清单、PREREG、REPORT 和 README 的结果表；每题记录（eval/results/*.jsonl）和 trace 不进 git，发布时打包挂 release。参考的是 Open LLM Leaderboard 把 details 放独立数据集、lm-eval-harness 的 log_samples 只推 Hub 不进代码仓库。push 前用 filter-branch 把早先进过历史的 jsonl 清掉。
- 2026-09-14 `effort: auto` 从 medium 起，重试用完先升 high 再 xhigh，升完才换大模型：多想一会儿比换模型便宜。
- 2026-09-14 加 effort 档位（low/medium/high/xhigh/max）：想不想、想多少 token、投票抽几个、重试几次、最多几步、要不要升级，全挂在 runner.py 的一张表上，medium 就是原来写死的那组数。入口是 lobes.yaml 的 `effort:`、`lobes ask --effort`、api 的 `reasoning_effort`。5090 上的 v1/v2 对比全在 medium，另跑一组 D 的 high 单独列。
- 2026-09-14 v2 起点：4070 上 v1 跑到 A、D 全量、E 一半时看了 trace，改动都对着具体失败项，规则写在 eval/PREREG-v2.md，v1 代码打了 tag `v1-4070`，两版都在租的 5090 上重跑才能比。评测结果目录加 `--tag`，一版一个目录。
- 2026-09-14 SimpleQA 改成有话直说：最终没过验证、或 PASS 但 basis 是 none 的答案，language 前面加 "Not sure. Best guess: "。v1 的弃权正则本来就抓 "not sure"，所以判分代码不动，报表多一列"答了且对"（correct 且没弃权）。v1 里 D 的 SimpleQA 弃权 0%、答了且错 63%，verifier 那条盲解在闭卷常识题上就是两个小模型互相猜，没意义。
- 2026-09-14 闭卷 qa（没工具输出、没图）reasoning 一律 3 采样：0.2 一个、0.7 两个，三个一致才给 basis consistency，否则 none 走 hedge。代码题如果题干自带 `>>>` 例子，第一个样本先过例子，过了就用，不过再抽两个挑过得最多的。用户要的"多跑几遍选最好"就是这两条，别的题不多跑。
- 2026-09-14 verifier 代码路径：先跑题干自带的 doctest 例子（代码判，不叫模型）；没例子才让模型写测试，而且不给它看代码，只给定义了哪些名字。v1 里 humaneval D 的 5 个错答案是"模型没写测试就 PASS"，4 个是看着代码写的弱测试放过了错代码。测试自己崩（NameError 在测试那几行）不算候选的错，按 `<string>` 行号分锅。
- 2026-09-14 `_compiles("61")` 是 True，所以 tools-08/-19 把一个数字当代码跑测试、PASS 了错答案。现在用 ast 看有没有 def/class/import/赋值/控制流才算代码。
- 2026-09-14 盲解和候选不一致又没东西可跑时：候选出现在某次成功的 stdout 里就 PASS（evidence），盲解答案出现在 stdout 里就换成盲解的（Verdict.answer，runner 套用）。tools-09 里 verifier 明明拿到了工具算出的正确答案，CONFLICT 却把错的候选留到最后。
- 2026-09-14 数字答案但所有 python 调用都挂了（多半是语法错）：evidence 直接 RETRY 一次让它修代码重跑。tools-02 就是工具挂了、reasoning 心算了个错数、gemma 返回空串被当弃权、PASS。
- 2026-09-14 python 工具：代码原样编译不过、把 `\n` `\t` `\"` 反转义后能编译就跑反转义的。granite 在 JSON 字符串里写字面量 `\n`，v1 里 A 43%、D 37% 的 python 调用死在这一个 SyntaxError 上。
- 2026-09-14 视觉链路改成三个读者：视觉模型描述+抄字、RapidOCR（代码，可选装 `lobes[ocr]`，结果按工具输出登记成 ocr_N 供 claim 引用）、verifier 再让视觉模型直接答一遍题。答案和 OCR 对上是 evidence，和第二眼对上是 consistency，第二眼和 OCR 对上就换答案。短边不到 768 的图先放大，v1 里 D 的 11 个 ocrbench 错题全是小图看错。
- 2026-09-14 reasoning 开思考后没返回 JSON（6000 token 用完），马上关思考再问一次。v1 里 A 的 SimpleQA 160 s/题大半耗在这。Reply 加 finish_reason 进 trace。
- 2026-09-14 Linux 上 b10951 没有 CUDA 预编译包（只有 cpu/vulkan/rocm/sycl），`lobes install` 在非 Windows 上 clone 对应 tag 用 cmake 编 llama-server（GGML_CUDA=ON，静态链接）。

- 2026-09-14 02:50 quick 计时：A（9B）平均 64 s/题，SimpleQA 上 160 s（盲解不一致后开思考，6000 token 用完还没答）；B（4B）24 s/题。按全量算 10 小时，超过一夜，按 PREREG 里写死的顺序砍：种子 1、2 只跑 multistep，GSM8K 和 SimpleQA 各 30 题，B3 不跑。预计 8 小时。改的是 eval.py 里的 N，判分和阈值没动。
- 2026-09-14 评测判分全是代码：GSM8K 取答案里最后一个数，HumanEval 跑官方 check()，SimpleQA 是标准答案字符串包含（官方用 LLM 判，我这个是下界），所以 SimpleQA 主要看“答了且错”的比例而不是准确率。等算力对照 B3 = 单 4B 从第一步就 3 采样投票，靠 cfg 的 vote 开关，不另写代码路径。
- 2026-09-14 单模型对照 A/B 直接当作 profile 写进 lobes.yaml（single-9b、single-4b），槽位填 none 表示没有。这样对照组和 specialists 走的是同一套 runner、语法约束、重试预算，代码里没有第二条路。
- 2026-09-14 verifier 盲解答“无法确定/could not be determined”时算弃权（PASS，basis 按有无工具证据），不再算 CONFLICT。gemma 对 fetch 任务连答 6 次同一句“无法确定”，把一个本来对的 Example Domain 送去了 9B 又送回来（193 s）。另外它提议的 python 检查如果只是 print 它自己的答案，当没提议。
- 2026-09-14 the 1.2B executive filed "take a screenshot" and "fetch <url>" as code, and the code path runs the answer as python, so both looped to the 9B and back (150 s, 227 s). Rule now: a tool verb in the goal downgrades the model's "code" to qa, and the code path only runs answers that compile.
- 2026-09-14 language 的改写要过代码检查：数字集合（去掉 goal 里出现的）必须和草稿一致，草稿 6 个词以内还得原样出现在改写里，否则丢掉改写用草稿。起因是 gemma 把验证过的 97405784 改写成了 97404784，等于最后一步把前面全白干了。
- 2026-09-14 python 工具在 stdout 为空、代码里没有 print、退出码 0 时，把最后一行当表达式重跑一次打印出来。granite 和 gemma 都爱写 `17 * 23` 然后等结果，空输出会让证据检查卡死在 RETRY。
- 2026-09-14 升级梯子：无思考 → 开思考 → 3 个 0.7 温度样本投票 → 9B → 远端（yaml 的 remote 槽，没 key 就跳过）。远端那级没 key 没跑过。
- 2026-09-14 `lobes api` 用 starlette + uvicorn，不装 fastapi。就两个路由，fastapi 的东西一样没用上。deepseek 不认 json_schema，provider 加 `json: object` 走 json_object 加把 schema 贴进提示；没 key 没验证。
- 2026-09-14 specialists 的 verifier 从 nemotron-nano-4b 换成 gemma4-e2b。同一份盲解提示 + JSON 语法下，nemotron 关思考 4 次里 2 次答 `,` 和 `: 391`，开思考把 goal 原句抄进 answer；granite 抄观察；gemma 4 次全答对还会提议 python 检查。副作用是 4B+gemma 一共 5.1G 能一起驻留，推理和验证之间不用来回换。nemotron 留在 yaml 里，评测阶段再比。
- 2026-09-14 验证 tool 类断言时，出现在 goal 里的数字不用工具输出背书，只查结果数。`17*23 = 391` 这种断言里 17 和 23 本来就不会出现在 stdout 里。
- 2026-09-14 verifier 的 PASS 只能由代码给：有工具证据是 evidence，盲解一致是 consistency，模型自己说“对”不算。Verdict.basis 记这个。
- 2026-09-14 8 个模型冒烟全过，显存按实测改了 yaml：gemma4-e2b 3900→1700，9B 6100→5200，其余小幅下调。qwen3.5-2b 开思考后 512 token 内没写完答案，perception 一律关思考。
- 2026-09-14 思考开关统一走 `chat_template_kwargs.enable_thinking`。Nemotron 3 Nano 的模型卡写的就是 `enable_thinking=False`，和 Qwen3.5 同一个机制，之前猜的 `/think` 系统提示删了。yaml 里 `thinking: true` 只表示这个模型有这个开关。
- 2026-09-14 `response_format` 用 OpenAI 形式 `{"type":"json_schema","json_schema":{"schema":...}}`。看了 b10951 的 server-common.cpp，两种写法都认，选 OpenAI 的是为了远端 provider 不用改。
- 2026-09-14 b10951 的 Windows 包里同时有 `llama-server.exe` 和 `llama.exe`，用前者，后者留着以防哪天前者没了。
- 2026-09-14 `lobes install --profile all` 而不是只装 specialists：shared 对照只多一个 4B 的 mmproj（0.67 GB），评测反正要用。
- 2026-09-14 yaml 流式映射里 `${VAR}` 必须加引号，`{` 会被当成 YAML 语法。
- 2026-09-14 晚：升级梯子和远端整个删掉。escalate / remote 两个槽、openai / deepseek provider、.env 和 key 的展开、评测条件 E / F、no_escalate 开关、记录里的 escalations 字段都没了；题难就交推理叶的值加"不确定"，不叫更大的模型，也不往外发。Qwen3.5-9B 留在 yaml 里只当评测基线 R。历史结果里的 E / F 列留在 REPORT 和 PREREG 不动。
- 2026-09-15 凌晨：multistep 19/30 输给裸 9B 的 25/30，trace 分两类。五道"写文件再报告"被 1B 判成 code，整条路交的是源码；四道多值题的一致判定只比最后一个数，运动叶少印或印错一个值也算和推理叶一致，交付的还是运动叶那段。改的是契约不是题：证人交的值一行一个、按题问的顺序，逐行一致（先打印中间量的只比末尾）；执行叶的 code 不再分流，推理叶先跑，题要源码时它填 code 字段，运行时才转代码路径，运动叶不再为代码题白跑。回放 v3 中档 370 道只改 4 道判定，全是错题。顺手修了 tools.py 的换行还原门：granite 把换行写成字面反斜杠 n，首行带 # 注释的一行程序能编译，第一句执行、其余全成注释，gsm8k 运动叶空输出 33 道里 23 道是它，改成没有真实换行时先试还原文本。v3 中档、高档重跑；裸 9B、第二版高档不用。
- 2026-09-15 早：值契约那版 multistep 24/30（裸 9B 25，之前 19），tools 前 12 道全对，可是每道 53 秒、13k token，其中推理叶开思考的那一次调用就占 11.3k token、46 秒，16 道里 13 道思考撞 6000 上限被迫作答。新错的四道里三道是这么来的：multi-20 思考把 16000 token 的单题上限烧穿，运动叶没轮到；multi-25 被迫作答后值是编的、复算程序空着；multi-31 三问只答了第一问。改回 ARCHITECTURE 一开始写的阶梯：推理叶先不开思考（值 + 复算程序），运动叶、验证叶跟上，三个便宜证人不一致才让推理叶开思考采 n 个样本；闭卷题同样先 n 个不思考的样本，连续 n 个一致才算，不一致再开思考采 n 个。n 的意思从"每题证人数"改成"思考样本数"。顺手一处：gemma 把 JSON 的 null 写成字符串 "null"，check 里这个字面被当程序跑了两次（NameError），现在当 null。v3 中档从 multistep、tools 重跑，值契约那 60 道留在 5090-v3-0778f73。
- 2026-09-15 上午：便宜证人先那版 60 道：multistep 23/30、tools 28/30，每题 8,040 / 2,679 token、34 / 7 秒（值契约版 13,058 / 9,481 token、54 / 36 秒）。翻新错的题，两处是一致判定自己的 bug，不是模型：`same()` 先删虚词再比，值是 "a"（最常见的词就是 a）两边都成空串，永远不一致；`restates()` 只看答案里的数是不是程序里的字面量，题面给的数（1900 到 2100、模 1000000）当然在程序里，于是复算程序根本没跑，模型写的值顶上；`[::-1]` 里的 -1 也算成了 1。改法：虚词删空就比原文；程序一律先跑，跑出来的数是程序里的字面量才算复述（题面的数是输入，只有输出说明问题）。中档的 16000 token 上限现在是便宜三个加一个撞满思考的样本（思考撞上限要补一次带前缀的调用，6000 个思考 token 记两遍），没动。留在 5090-v3-1c696ac，再重跑。
- 2026-09-15 上午，再一轮：判定修好那版 multistep 25/30、tools 27/30。tools-34（解 5x − 3 = 2x + 21）运动叶的程序算出 8，推理叶和验证叶不开思考都写 7，复算程序是 `x = 7` 这种空话，两个没跑程序的证人一致，按"两个一致就定案"压过了跑出来的 8，思考那一档根本没轮到。这违背 README 第一段的规矩：程序打印的比模型写的可信。settle 加一条：只要有证人跑过程序，定案的那一对里必须有一个跑过的。另外 JSON 解析失败时原来把模型原文当值（旧 answer 契约的遗留），2^100 那题推理叶吐了两千个数字，原文和运动叶的输出被判成一致；现在解析失败就没有值。PREREG-v3 的"两个一致"由此多了一个限定，记进 REPORT 的偏离。
- 2026-09-15 上午，第三轮：settle 那版 multistep 23/30、tools 29/30，比上一版少两道 multistep，翻错的五道逐条看：multi-23（数元音、辅音、最常见字母）运动叶的值是 "wrote 71 chars to s.txt"，验证叶写了四行废话最后一行是 "e"，same() 里的子串匹配把 "e" 算进 "wrote 71 chars" 里，两个"一致"就 PASS 了——单字母答案（最常见字母、选项 A/B）碰上任何含这个字母的句子都会撞上，是契约 bug 不是题目问题，改成整词匹配（一边的词集是另一边的子集才算，"paris" 对 "the capital is paris" 还在）。multi-37（json 里所有数之和、键数、深度）运动叶的程序算出 211/17/6，两个思考证人写的 211/11/5 是对的但程序一个失败一个只打印字面量，按新规矩没跑的压不过跑的，判成 conflict；上一版是靠"两个一致"定案对的。tools-34 和 multi-37 一正一反各一道，规矩不为这一道改：程序输出比模型写的可信这条是 README 第一段，模型写对了程序没写对，正确的出路是证人把程序写对。multi-34/35 是运动叶两次调用都失败加 token 上限，上一版同样代码是对的，噪声。multi-20 两版都错。
- 2026-09-15 上午，第四轮：便宜证人先在 gsm8k 上的账算出来了。同样 198 道（5090）：裸 9B 183、旧 v3 中档（推理叶思考常开）179、便宜证人先 169，token 一题 10,924 / 6,804 / 4,578。掉的十道里九道是推理叶和运动叶两段不思考的程序读错同一处题面、算出同一个错数，两个一致就定案了：同一家族的两个不思考的 4B 错误相关，这时"两个一致"不是独立证据。多步题和工具题上便宜证人先和旧版打平或略好，只有 gsm8k 掉。问过用户：要分不要省 token。改回推理叶一上来就按档位思考（第一个样本 0.2，热样本 0.7，思考样本一共 n 个），运动叶和验证叶仍不思考，settle 规则、一行一值、same/restates 修都留着。测试 23 过。这一版的 v3 中档和高档重跑。
- 2026-09-15 中午，收尾：思考版两档都跑完（中档 291/370、高档 297/370；裸 9B 跑的 320 道上 261 / 267 对 268，token 67% / 107%，秒 61% / 104%）。翻过全部错题和撞上限的题的 trace：错的是两个证人同错（推理叶加运动叶、推理叶两个思考样本、验证叶跟着错），撞 token 上限的（中档 27、高档 21）全是四个证人四个值到第五个样本才被截；用记录的证人重算，去掉热样本只算前三个证人，中档 gsm8k 少对七道、高档少对三道，热样本没拖后腿。没有契约 bug，不重跑，上限尺寸的来历写进 REPORT 偏离 15。定稿：README 两份只留放大后题库的成绩（裸 9B、v3 中档、v3 高档，图里另有换上 v3 归类的第二版高档），里程碑按同一批题重算（第二版中档 234、第二版高档 241）；结果目录里的 commit 哈希改成说人话的名字（5090-v3-last-number、-values、-cheap-first、-cheap-first-2、-settle、-settle-words、-old-intake、-high-old-intake）；eval/pod.sh 里的旧哈希改成文字，第二版高档用分支 v2-fix；ARCHITECTURE 评测一节回填数字和假设的结论。
- 2026-09-15 下午，证人机制第二轮（用户定方向：先修机制本身）。四件事都先量了再定：(1) 证人程序字段叫 check，中档 765 段程序里 90 段写成 `def check(...)` 从不调用、88 段跑完没输出，高档 108 / 845，每段白费一次修复调用和一次运行，证人退化成写出来的值，gsm8k-1056、711 这样错的；改名 program，说明加一句"顶层语句、没人调用的函数不打印"，测试 23 过，两档在 5090 重跑（5090-v3-program、-program-high）。(2) 要求一致的两个证人必须来自不同脑叶：用记录的证人重算，中档 gsm8k 175 对 177、高档 182 对 182，定不了案的题翻倍，不改。(3) 硬 token 上限（预留余量就不再抽证人）：用 trace 模拟，gsm8k 丢 2 到 3 道省 9%，multistep 不丢省 20%，但一个强制样本自己就 12.7k，怎么留余量都有溢出；上限尺寸是取舍不是机制，不动，W4 照实记不成立。(4) 强制作答重发思考：llama-server `--reasoning-budget` 服务端有效、每请求无效（5090 实测），裸 9B 同样重发（320 道里 147 道强制），账对称，不改。(5) 代码题没有例子时验证器盲写的测试：两档 34 份里 21 份把被测函数自己重写了一遍，测的是验证器自己（11 次假的 evidence、7 次把对的候选驳回，HumanEval-117 高档四次重试全耗在这上面），3 份 JSON 里的 `'\n'` 变成真换行不能解析（HumanEval-125 两档都没测就交了），10 份真测试里 2 份把对的代码判错；剥掉重写之后断言又判错 10/25，验证器自己那份实现只对 14/21。四个错的首版候选它一个没真抓到。删了：没有例子就不查，basis none 交出去；两档 program 重跑的 humaneval 30 题用删后的代码补跑进同一目录（代码路径只在 humaneval 进过，逐 trace 数过）。
- 2026-09-15 傍晚，第三轮（用户定：不换模型、不给题库做特殊优化，提升必须是系统性的；可参考 OpenAI 开源的 evals / simple-evals / grade-school-math 的做法）。四件事都是从已有 trace 量出来再改的，都不读题库的答案格式：(1) 验证叶不再当证人。5090 两档 gsm8k 它盲解 143 次对 17 次（中档 6/68、高档 11/75），它和运动叶一致定案的 10 对全错，中档有 3 道（1267、450、1032）是推理叶对、被它俩压掉；一个对 9% 的证人的赞同不是证据，删掉比修便宜。计划变成推理叶思考、运动叶、推理叶热样本。(2) 单题 token 上限按生成 token 算：原来比的是 total_tokens，一次被截断的思考样本连预填重发记两遍（中档 12.7k），第二个证人就撞 16k，340、506、1070、611 第二个样本已经对了却没抽到第三个就 hedge。表里的数不动。(3) 图片题定案的两个证人里必须有感知叶：推理叶看不到图，每个样本读同一份笔记，两个样本一致等于一次阅读数两遍，两档各 3 道（359、281、176）就是这样把对的感知叶压掉的；定不下来交感知叶的值带 hedge，不再交推理叶的。(4) 代码题的例子解析：`f(x) => y`、`f(x) ➞ y`、`f(x) == y` 写在文档字符串里的算例子，`>>> f(x) == y` 没写期望输出的按 True 算；30 道 humaneval 里 9 道只有这种写法（连 `==>` 一起算；另 5 道没有能读的例子）、145 的两条 >>> 之前对任何实现都判失败。跑之前的两版 humaneval 错的 125、145、163 全是这三类。simple-evals 的做法（零样本 CoT、答案放最后一行、HumanEval 每题抽 5 个样本算 pass@1）和 grade-school-math 的多数表决都看了：我们的"两个一致才定案"本来就是提前停止的多数表决，没照搬 n=5 抽样，token 翻倍。新 tag 5090-v3-witness / -witness-high，另跑一遍同码中档 5090-v3-witness-2 量噪声。pod1 同时在量：同一套 2B 感知叶五种问法各对多少（直接答两次、热样本、思考、先转写再答）、4B 推理叶每题 1 冷 4 热 2 不思考 + 运动叶 1 份样本，用来回放定案规则；量出来再决定要不要动热样本的数目。感知叶那组量完了（50 道 ocrbench，2B）：直接答 32、再抽一次 31、热样本 33、先转写再答 29、推理叶读笔记 31、开思考 34/45（5 道被服务拒掉），三种问法的并集 37；按正式定案规则回放，感知叶开思考领头 35，后面跟推理叶、直接答、热样本都一样，现在的排法 32。改成感知叶按档位思考——推理叶在文字题上的同一条规则，不是给图片题单开的——思考调用被拒就不思考再要一次（Ctx.chat 里，各叶共用）。被拒的原因：2B 的 llama-server 四个槽共用一个 16384 的统一 KV，四路并发各带图再思考到 6000 就溢出（serve.log：Context size has been exceeded）；正式跑分三次 370 道 0 报错是因为感知叶原来不思考。手写公式那 8 道（gold 每个字符之间有空格）六种问法全错，判分不改，仍算错。同一批 trace 里另外量了两件没改的事：运动叶（granite h-micro）的程序中档 284 次里失败 34、高档 313 里 45，约七成是格式而不是算错（JSON 字符串里换行丢了、`\n` `\"` 双重转义、尾巴带 `}}` 或 `[/code]`），修一次还失败的题中档 16 道、高档 21 道，可这些题最后中档 12 对 4 错、高档 18 对 4 错（错的里 simpleqa 各占 2），热样本补上了；让它写代码块不塞 JSON 能少掉这类失败，量出来的分数收益最多一两道，不动。推理叶自己列的 values 和它的程序打印的值不一致时（中档 gsm8k 5/194、multistep 17/40、tools 7/30）：gsm8k 各半，multistep 和 tools 几乎都是程序对、心算错（中档 15/17、7/7），「程序输出就是值」这条规则是对的，不动。代码题另外两处（program 中档 26/30 的四道错里两道）：HumanEval-117、106 是执行叶判成 code 之后推理叶把整个函数写进了 values 而不是 code 字段，证人流程对源码永远一致不了，最后 hedge 前缀「Not sure. Best guess: 」贴在代码前面成了语法错误；执行叶的 code 判断 humaneval 30/30 全中但 multistep 5 道、tools 3 道也被判成 code，所以不能只按它路由——现在是执行叶说 code 且第一个证人的值本身是源码才走代码路径，hedge 永远不贴在代码前面。HumanEval-65 的文档例子写的是 "21" 而 repr 是 '21'，对的代码两个例子全判失败（每次跑都这样，交出去时 basis none）：期望值能解析成字面量的按值比较（`f(x) == want` 期望 True），不再比文本。
- 2026-09-15 夜，PREREG-v4（题库放大而不是再改机制）。v3 收尾时四个要比的数挤在 6 道的带子里、tools 连着两版 30/30，这种题库量不出东西，所以 tools 和 multistep 各加 30 道更难的，老的 30 道一个字节没动（和备份 diff 过前 30 行），新题的每个期望值都由 eval/suites/make.py 算出来，没有一个是手敲的；另外加了 AIME 2025（不是 2024，2024 进训练集两年了）。加完先跑自检才冻结，当场抓到两个坏题：multi-48 的 gold 是 "a"，judge 的 norm() 把虚词删空，空串匹配一切答案，任何答案都算对；multi-43 题面说平局取较大的数字、代码写的是较小的。自检留在 eval.py 的 __main__ 里，规则是"用它自己的答案必须过、用空答案必须不过"。2026! 的阶乘换成 1400!，因为 Python 3.11 的 int→str 有 4300 位上限，那道题会变成考 Python 版本而不是考算力。AIME 先跑 pilot 看有没有坐地板（30 道对 16，没有），再冻结。**不重跑没变的题库**：gsm8k、humaneval、ocrbench、simpleqa 和两个题库的老一半全部沿用 v3 的记录——`git diff aded056 2bb09e1 -- lobes/` 只有两个 docstring，重跑只是对 v3 已经采过两遍的分布再采一遍。实现方式是把 v3 的记录按条件各自灌进 v4 的结果文件、打 carried 标记，lobes eval 本来就按 suite+id 跳过已有的记录。（第一次发车时我按六个题库全跑了，12 分钟后用户叫停，两台机约 15 分钟机时白烧，partial 留作 5090-v4-aborted-fullsuite，一个数都没读。）不加"9B 也能调工具"这个对照：比的就是"你实际会怎么跑这个模型"，不是这套运行时自己的消融版；代价写在 PREREG-v4 里——tools 那 19 分里有多少是架构、有多少是有 python，这套评测分不开，报告里不许写成"六个脑叶打一个脑子"。结果：新的 tools 30 和 28 对裸 9B 的 11，新的 multistep 25 和 28 对 20，两遍中档差 4 道；成本方向在这批题上反过来了，60 道上 2.45 倍 token、1.25 倍秒，v3 那句"59% 的 token"只对它量过的题成立，两份 README 都写清楚了。AIME 三个中档样本 16、12、15，裸 9B 13 落在里面，高档 16 也落在里面：这是这套设计的边界——AIME 奖励一条拉得很长的推理链，正是大模型自己思考擅长、而拆给互相看不见的证人不擅长的东西；照实记进 REPORT，README 不放。七条假设三条成立，V7（档位旋钮在 AIME 上终于能分开）第三次不成立。
- 2026-09-16 公开前通读仓库，工具层三处对不上：(1) ARCHITECTURE 写「read_file / write_file / edit_file 只在工作目录」，实际只有写和编辑过 `_inside()`，read_file 的绝对路径分支直接读，机器上任何文件都能读出来——看着是漏了不是设计，现在三个都过 `_inside()`；题库里那五道「写文件再读回来」全在工作目录内，不受影响。(2) `shell` 那条 BLOCKED 正则（rm/del/format/shutdown/dd/reg…）删了，工具描述里「destructive ones are blocked」那句也删了：实测挡住 `rm -rf /`，却放行 `python -c "..."` 和 `$(...)`，还误伤 `echo hi && echo rm`；一个挡得住手滑、挡不住别的东西的名单，最大的坏处是让人以为有防护。现在不声称任何防护。(3) README 两份从头到尾没提过 motor 叶会在本机执行代码，Quick start 第四行就是 `lobes ask`；加了一段说清楚 python 和 shell 用你的身份跑模型写的任何东西、只有 10 秒超时，文件工具锁在工作目录、这两个不锁，不信任的机器把整个进程放容器里。tools.py 的自检把 `rm -rf /` 那条换成「读工作目录外的绝对路径必须失败」。顺带：pyproject 的 description 是一句堆形容词的功能清单（"An experimental modular AI runtime that coordinates specialized small-model lobes with evidence-based verification and dynamic model swapping"），跟 README 的语气不是一个人写的，换成一句人话；CI 补上 `python -m lobes.eval`（判分自检，抓 multi-48 和 multi-43 的那条，之前只在本地手跑）；DECISIONS 上一条把 1.25 倍秒写成了 1.27，改正。

- 2026-09-16 `nemotron-nano-4b` 从 lobes.yaml 删掉。09-14 verifier 换成 gemma4 时留着它「评测阶段再比」，三轮评测过去了一次都没进过 profile，`lobes install` 却照样为它下 3.2 G。名单里现在每个模型都有 profile 引用它。两份 README 的长段落顺手收了一遍（英文正文 91 → 83 行），开头加了一行目录。
