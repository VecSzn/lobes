.env
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

# downloaded by `lobes install`, never committed
bin/
models/
*.gguf

# per-task traces
runs/

# the goal prompt driving the build, not part of the project
.goal.md

# eval sources, 70 MB, fetched by `lobes eval`
eval/data/

# per-item eval records; the tables in eval/REPORT.md are computed from them, the files ship as a release asset
eval/results/
docs/img/*.png
